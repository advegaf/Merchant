// Legacy file - removed to avoid conflicts
// Use RootView and HomeView from UI/ folder instead

import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Deprecated - Use RootView")
    }
}
